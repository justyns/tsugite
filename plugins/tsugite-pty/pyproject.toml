[project]
name = "tsugite-pty"
version = "0.21.0"
description = "Tsugite plugin: PTY terminal runtime and tools (daemon-only)"
authors = [{ name = "Justyn Shull" }]
requires-python = ">=3.11"
dependencies = ["tsugite-cli==0.21.0"]

[project.entry-points."tsugite.plugins"]
pty = "tsugite_pty.tools"

[project.entry-points."tsugite.commands"]
pty = "tsugite_pty.commands"

[project.entry-points."tsugite.context_providers"]
pty = "tsugite_pty.context"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tsugite_pty"]

[tool.hatch.build.targets.sdist]
include = [
  "/tsugite_pty",
  "/pyproject.toml",
]

[tool.uv.sources]
tsugite-cli = { workspace = true }
