# v0.19

## 0.19.2 (unreleased)

### Fixes

- Attaching a shell script, JSON, SQL, or other code file with `-f` now gives the model the file's text instead of an empty-looking base64 document.
- `<!-- tsu:tool -->` directive and `prefetch` arguments can now reference variables assigned by earlier steps, earlier directives, and earlier prefetch entries, instead of failing with `'x' is undefined`.
- `post_tool` hooks now receive the same workspace-relative path the tool wrote. Sessions running below the workspace root used to get the cwd prefixed onto it, so path-based hooks fired on files that did not exist.
- `tsu daemon` client commands now default to port 8374, the port the daemon actually serves, so they work with no flags. `--host` / `--port` / `--token` also read `TSUGITE_DAEMON_HOST` / `TSUGITE_DAEMON_PORT` / `TSUGITE_DAEMON_TOKEN`.
- One-off `run_at` schedules now honor the entry's `timezone` for times written without an offset, matching cron. A reminder set for 8pm Central used to fire at 8pm UTC. The same fix applies to `expires_at`.
- A session no longer gets stuck showing an in-flight status label (`Waiting on LLM...`) in the sidebar when nothing is running. This also unblocks scheduled compaction of affected sessions, which the stale label suppressed indefinitely.
- Sensitive tool arguments are redacted by key and by path, not only when the value came from `get_secret()`. `Authorization`, `Proxy-Authorization`, `Cookie`, `Set-Cookie`, `X-API-Key`, `X-Auth-Token` and `X-CSRF-Token` render as `Bearer ***` / `***` wherever they appear in tool-call arguments (live events, persisted history, replay), and `http_request` / `check_url` redact `Set-Cookie` in the response headers they return. Tool authors can declare their own with `@tool(sensitive_args=["headers.Authorization", "body.password"])`.
- `codex_cli` lists models again, and offers the GPT-5.6 family (`gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`) with their `max` / `ultra` reasoning efforts. The provider was reading the Codex `/models` response with the wrong key, so every picker showed an empty list. Retired `gpt-5.4-nano`, which the backend no longer offers.
- A chat's `N jobs` chip now opens the jobs board filtered to that chat's jobs (`#jobs?q=session:<id>`). The board filter lives in the URL, so the link is shareable, survives a reload, and clears from the board's search field.
- The chat header's `compacting` pill now tracks real compactions instead of guessing from status text. It used to light up only if a workspace hook happened to mention compaction, and lit up spuriously when an unrelated hook message did.

### Full changelog

https://github.com/justyns/tsugite/compare/v0.19.1...HEAD

## 0.19.1 (2026-08-03)

### Fixes

- Overlapping requests no longer fail authentication at random with a valid token.
- Concurrent agent runs no longer share each other's secret and agent scoping.
- Streaming and non-streaming turns report the same token usage and cost.
- Strict OpenAI-compatible endpoints like Fireworks no longer reject requests over Tsugite's prompt-cache markers.
- Prompt caching works again on `anthropic:`.
- A late verifier from a superseded attempt no longer errors a job that has already been retried.
- Answering a blocked job keeps the question when the answer cannot be delivered.
- A failed worktree cleanup no longer loses the path, so leftover job directories stay trackable.

### Changes

- History is stored in SQLite only; the deprecated `jsonl` backend setting now errors.

### Full changelog

https://github.com/justyns/tsugite/compare/v0.19.0...v0.19.1

## 0.19.0 (2026-08-01)

### Features

- Rebuilt web UI in Svelte 5 ("Operational Console"): ⌘K command palette, dockable split-pane workspaces, five themes, and new Secrets, Plugins, Tools, and workspace-files views.
- Image and photo uploads reach vision-capable models, with a mobile camera button, inline thumbnails, and a lightbox.
- "Add context" menu attaches structured context as chips: browser location, `@` workspace files, and plugin-provided sources.
- Web-fetched context is marked untrusted and gated on per-host approval.
- Attach a session, job, or terminal to a chat.
- Live token-by-token streaming of chat turns, including `python-exec` code and per-tool-call rows.
- Reasoning/thinking is persisted to history.
- Claude Opus 5 in the model registry (`claude_code:opus-5`).
- Slash commands in the ⌘K palette, contributable by plugins, with typed argument autocomplete.
- `/model` and `/effort` set per-session overrides (web and Discord).
- More keyboard shortcuts with a `?` overlay, and "New chat" from any view.
- Raw-messages inspector for any past session.
- VSCode-style file preview tabs.
- Load agents from multiple directories (`agent_paths`); group agent `attachments:` into prompt-cache tiers.
- `spawn_agent`/`spawn_job` accept `files=[...]`.
- Usage tab breaks out scheduled tasks and adds cache columns plus a per-reply cache footer.
- Session-source tags (web/discord/cli) and new endpoints (`POST /api/jobs`, `GET /api/plugins`, `GET /api/tools`).

### Fixes

- Removed the reactions feature.
- Background spawns, jobs, and chat routing resolve the correct daemon agent again.
- Claude Code chats self-heal when a resumed transcript becomes unresumable.
- Approval prompts are durable and survive reloads.
- Conversations open fast and stay live across tabs, schedules, and Discord.
- Per-turn token usage persists again, and `anthropic:` no longer crashes after the first turn.
- Ended chats collapse into their own searchable, restartable section.
- Model picker is grouped by provider with context, price, and capability details.
- Streaming no longer fights your scrolling.
- Many mobile fixes: escapable full-screen views, drill-down navigation, reachable Settings and effort controls, Enter inserts a newline, and drafts/photos persist across reloads.

### Full changelog

https://github.com/justyns/tsugite/compare/v0.18.0...v0.19.0
